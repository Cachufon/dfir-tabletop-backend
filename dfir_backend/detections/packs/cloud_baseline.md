# Cloud Baseline Pack

Use this pack for cloud infrastructure detections across providers.

- Reference `dfir_backend/rules/sigma/` for cloud-related rules.
- Typical categories: IAM changes, suspicious actions, and access anomalies across cloud resources.
- Layer in environment-specific rules from `dfir_backend/custom/<case_id>/` for provider nuances.
