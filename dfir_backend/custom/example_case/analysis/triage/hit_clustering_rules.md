# EXAMPLE Hit Clustering Rules

- Cluster by `user` and `src_ip` within 30-minute windows to associate MFA approvals with subsequent logins.
- Group SaaS configuration changes that share the same session identifier or IP to connect mailbox changes to the login.
- De-duplicate repetitive MFA prompts with identical outcomes to reduce noise while retaining volume indicators.
