# EXAMPLE Storyline Candidates

1. Attacker triggers repeated MFA prompts to `alice@example.com` until approval occurs.
2. Successful session from `198.51.100.42` uses the approved MFA to access mail.
3. Same session creates a forwarding address and inbox rule to send copies of mail to `exfil@example.net`.
