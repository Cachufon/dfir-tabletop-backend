# EXAMPLE Detections Rollup

- **Identity:** MFA fatigue followed by successful login from `198.51.100.42` for `alice@example.com`.
- **SaaS:** Mailbox forwarding and rule creation actions from the same session indicate potential data exfiltration setup.
