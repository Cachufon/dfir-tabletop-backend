# EXAMPLE SaaS Detection Summary

- Mailbox forwarding rule created to `exfil@example.net` immediately after foreign login.
- SMTP forwarding address updated to an external domain from the same source IP.
