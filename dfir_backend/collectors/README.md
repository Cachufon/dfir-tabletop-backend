# Collector Stubs

Collector stubs define the expected inputs and normalized outputs for each scope area. They serve as vendor-agnostic interfaces rather than implementations, guiding how ingestion should map into the [normalized data contract](../normalization/normalized_data_contract.md). Each subdirectory documents what data must be provided, what optional enrichments are supported, and the standardized schema produced.
