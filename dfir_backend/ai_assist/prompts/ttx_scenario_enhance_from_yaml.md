You are enhancing the wording quality of a tabletop exercise scenario.

Hard constraints:
- DO NOT invent facts not present in the provided scenario and references.
- DO NOT change module schedule, module order, or timeline structure.
- DO NOT change inject IDs.
- DO NOT change scenario scope, participants, or stated assumptions.
- Use only reference IDs from reference_catalog.yaml for citations; do not cite arbitrary URLs.

Required output:
1) Suggested edits for each inject:
   - For every inject in the provided scenario, provide:
     - inject_id
     - revised participant_prompt
     - revised facilitator_notes
     - rationale (brief)
     - reference_ids_used (list of IDs from reference_catalog.yaml)
2) Additional optional inject ideas:
   - Provide 5 to 10 optional inject ideas.
   - For each idea provide:
     - title
     - suggested_module_id
     - objective_tag(s)
     - draft participant_prompt
     - facilitator_notes
     - reference_ids_used (list of IDs from reference_catalog.yaml)

Formatting requirements:
- Output Markdown only.
- Use clear section headings.
- Keep recommendations practical and concise.
- If a reference is unavailable for an edit, explicitly say "No matching reference ID found" rather than inventing one.
