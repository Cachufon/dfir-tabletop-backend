# Sigma Rules

This folder stores Sigma rules in native YAML format. These rules focus on log and event-based detections for identity, SaaS, and endpoint telemetry. Many rules will be sourced or adapted from community projects like SigmaHQ, Elastic, Azure Sentinel hunting queries, and Okta detection rules. Analysts should edit rules here using standard Sigma syntax so they remain compatible with the broader ecosystem.
