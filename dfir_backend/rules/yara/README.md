# YARA Rules

This folder stores native YARA rules for binaries, memory artifacts, and certain file-based detections. YARA here is primarily used to support macOS investigations and endpoint compromise assessments. Rules should follow standard YARA syntax and be compatible with common YARA tooling.
