# NOVA Prompt Rules

This folder contains AI prompt detection rules using the NOVA framework syntax (prompt pattern matching system). These rules are used to detect adversarial or abusive prompts (IoPCs) in AI logs, inspired by PromptIntel and NOVA. Rules follow NOVA’s native rule format (similar to YARA but with meta, keywords, semantics, llm, and condition sections).
